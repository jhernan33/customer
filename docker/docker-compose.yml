version: '3'
services:
    ci4-web:
        build:
            context: ./php
        ports:
            - 80:80
        volumes:
            - .:/var/www/html/
networks:
  default:
    external:
      name: external-example